<!doctype html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Register</title>
    <link href="css/styles.css" rel="stylesheet" />
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Mitr:wght@200;300;400;500;600;700&family=Sriracha&display=swap" rel="stylesheet">
    <link rel="canonical" href="https://getbootstrap.com/docs/5.0/examples/sign-in/">

    <!-- Bootstrap core CSS -->

    <!-- Favicons -->
    <link rel="apple-touch-icon" href="/docs/5.0/assets/img/favicons/apple-touch-icon.png" sizes="180x180">
    <link rel="icon" href="/docs/5.0/assets/img/favicons/favicon-32x32.png" sizes="32x32" type="image/png">
    <link rel="icon" href="/docs/5.0/assets/img/favicons/favicon-16x16.png" sizes="16x16" type="image/png">
    <link rel="manifest" href="/docs/5.0/assets/img/favicons/manifest.json">
    <link rel="mask-icon" href="/docs/5.0/assets/img/favicons/safari-pinned-tab.svg" color="#7952b3">
    <link rel="icon" href="/docs/5.0/assets/img/favicons/favicon.ico">
    <meta name="theme-color" content="#7952b3">

    <style>
      .bd-placeholder-img {
        font-size: 1.125rem;
        text-anchor: middle;
        -webkit-user-select: none;
        -moz-user-select: none;
        user-select: none;
      }

      @media (min-width: 768px) {
        .bd-placeholder-img-lg {
          font-size: 3.5rem;
        }
      }
    </style>

    <!-- Custom styles for this template -->
    <link href="/css/signin.css" rel="stylesheet">
</head>
<body class="text-center">
    
<main class="form-signin">
  <form action="/user/register" method="POST">
    <h1 class="h3 mb-3 fw-normal">สมัคลงทะเบียน</h1>

    <% if (validationErrors && validationErrors.length > 0) { %>
      <% validationErrors.forEach(error => { %>
        <div class="alert alert-danger" role="alert">
          <%= error %>
        </div>
      <% }) %>
    <% } %>

    <div class="form-floating">
      <input type="text" name="username" value="<%= data.username %>" class="form-control" id="floatingUsername" placeholder="Username">
      <label for="floatingUsername">ชื่อผู้ใช้งาน</label>
    </div>
    <div class="form-floating">
      <input type="email" name="email" value="<%= data.email %>" class="form-control" id="floatingInput" placeholder="name@example.com">
      <label for="floatingInput">อีเมล</label>
    </div>
    <div class="form-floating">
      <input type="date" name="birthdate" value="<%= data.birthdate %>" class="form-control" id="floatingBirthdate" placeholder="Birthdate">
      <label for="floatingBirthdate">วันเดือนปีเกิด</label>
    </div>
    <div class="form-floating">
      <select class="form-select" name="gender" id="floatingGender">
        <option value="" disabled <%= !data.gender ? 'selected' : '' %>>เลือกรายการ</option>
        <option value="male" <%= data.gender === 'male' ? 'selected' : '' %>>ชาย</option>
        <option value="female" <%= data.gender === 'female' ? 'selected' : '' %>>หญิง</option>
        <option value="other" <%= data.gender === 'other' ? 'selected' : '' %>>อื่นๆ</option>
      </select>
      <label for="floatingGender">เพศ</label>
    </div>
    <div class="form-floating">
      <select class="form-select" name="underlyingDisease" id="floatingUnderlyingDisease">
        <option value="" disabled <%= !data.underlyingDisease ? 'selected' : '' %>>เลือกรายการ</option>
        <option value="yes" <%= data.underlyingDisease === 'yes' ? 'selected' : '' %>>มี</option>
        <option value="no" <%= data.underlyingDisease === 'no' ? 'selected' : '' %>>ไม่มี</option>
      </select>
      <label for="floatingUnderlyingDisease">โรคประจำตัว</label>
    </div>
    <div class="form-floating">
      <select class="form-select" name="educationLevel" id="floatingEducationLevel">
        <option value="" disabled <%= !data.educationLevel ? 'selected' : '' %>>เลือกรายการ</option>
        <option value="bachelor" <%= data.educationLevel === 'bachelor' ? 'selected' : '' %>>ปริญญาตรี</option>
        <option value="master" <%= data.educationLevel === 'master' ? 'selected' : '' %>>ปริญญาโท</option>
        <option value="doctorate" <%= data.educationLevel === 'doctorate' ? 'selected' : '' %>>ปริญญาเอก</option>
      </select>
      <label for="floatingEducationLevel">ระดับการศึกษา</label>
    </div>
    <div class="form-floating">
      <input type="password" name="password" value="<%= data.password %>" class="form-control" id="floatingPassword" placeholder="Password">
      <label for="floatingPassword">รหัสผ่าน</label>
    </div>
    
    <button class="w-100 btn btn-lg btn-primary" type="submit">ยืนยันการลงทะเบียน</button>
    <p class="mt-5 mb-3 text-muted"></p>
    <a class="btn btn-sm btn-success" href="/">เข้าสู่ระบบ</a>
  </form>
</main>

</body>
</html>
